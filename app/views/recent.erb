<ul class="haikus">
  <li ng-repeat="haiku in haikus">
    <div class="haiku">
      <h3>{{ haiku.user.name }}</h3>
      <p class="first">{{ haiku.first }}</p>
      <p class="second">{{ haiku.second }}</p>
      <p class="third">{{ haiku.third }}</p>
    </div>
  </li>
</ul>